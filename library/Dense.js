class Dense extends Layer {
    constructor(inputSize, outputSize) {
        super();
    }

    forward(input) {
        super.forward(input);
    }

    backward(outputGradient, learningRate) {
        super.backward(outputGradient, learningRate);
    }
}
